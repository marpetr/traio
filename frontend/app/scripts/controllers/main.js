'use strict';

/**
 * @ngdoc function
 * @name traioApp.controller:MainCtrl
 * @description
 * # MainCtrl
 * Controller of the traioApp
 */
angular.module('traioApp')
  .controller('MainCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
